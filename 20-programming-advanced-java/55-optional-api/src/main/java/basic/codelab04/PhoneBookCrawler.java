package basic.codelab04;

public class PhoneBookCrawler {
    private PhoneBook phoneBook;

    public PhoneBookCrawler(PhoneBook phoneBook) {
        this.phoneBook = phoneBook;
    }

    /**
     * Implement this method so that it returns the phone number
     * if you can not find a phone number return the whole phone book as a string
     * !! Make sure to only call the phoneBook toString method in case you can not find the phone number !!
     * The test will fail if you call the toString method in the other case
     */
    public String findPhoneNumberByNameAndPrintPhoneBookIfNothingFound(String name){
        return null;
    }
}
